# SmartServe AI — backend dependencies.
#
# This is a scoped list of what the Django project actually imports, not a
# dump of the local dev environment. If you add a new import anywhere in the
# codebase, add its package here too — do not `pip freeze > requirements.txt`
# from a shared/global Python environment, since that pulls in every unrelated
# package installed there (this is what caused a broken deploy once already:
# an unrelated Python-3.13-only package blocked the whole build).

# ── Django core ──────────────────────────────────────────────────────────────
Django==6.0.5
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
PyJWT==2.13.0
django-cors-headers==4.9.0
python-decouple==3.8

# ── Database drivers ─────────────────────────────────────────────────────────
pymongo==4.17.0
psycopg2-binary==2.9.11

# ── Data / ML (ml_engine, analytics, reports) ───────────────────────────────
pandas==2.3.3
numpy==2.4.0
xgboost==3.2.0

# ── Report exports (reports/views.py) ───────────────────────────────────────
openpyxl==3.1.5
reportlab==5.0.0

# ── Media uploads (accounts.models: User.avatar, Business.logo) ────────────
Pillow==12.1.0

# ── Production server / static files ────────────────────────────────────────
gunicorn==26.0.0
whitenoise==6.8.2
