# Base URL of the Django REST API. No trailing slash.
# Must match where `python manage.py runserver` is listening, and this origin
# must be listed in the backend's CORS_ALLOWED_ORIGINS.
VITE_API_URL=http://127.0.0.1:8000/api
